# jkcastudio — a JKCA build

**Come into JKCA first.** Before deciding or pivoting into any lane, load the house frame:
1. House law: `~/Desktop/CLIENTS/CLAUDE.md` (framework, build standards, hard rules — read it)
2. Operator tiers: `~/Documents/GitHub/jkca-toolkit/playbooks/OPERATOR.md` — Tier A act unattended / Tier B prepare + present / Tier C never
3. Bars before building: toolkit `playbooks/DESIGN-RULES.md` + `VOICE-RULES.md`; deps off `TOOLBOX.md` only; any launch runs `playbooks/LAUNCH.md` (hard gates incl. asset provenance + private-data gate)
4. Client context: `~/Desktop/CLIENTS/_Active/JKCA/CLAUDE.md` + `STATUS.md` — update STATUS when work ships (the board reads it)

Non-negotiables here: Cloudflare "JKCA Stacks" is PRODUCTION (read-only by default; deploys are Tier B: verify locally, never push unverified). Raw shoots/brand sources/client docs stay in CLIENTS — never commit them to this repo. No off-system design values. Invent nothing.

No AI authorship: never add Co-Authored-By / Generated-with trailers to commits, PRs, or files in this repo (Json, 2026-07-09).
