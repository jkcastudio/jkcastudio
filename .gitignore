# build output
dist/
.astro/

# dependencies (node_modules is a symlink to node_modules.nosync — bare, no trailing slash)
node_modules
node_modules.nosync

# cloudflare / wrangler local state
.wrangler/
.dev.vars
.dev.vars.*

# ── JKCA secrets, never commit (security-audit canonical block) ──
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.token
credentials*
secrets*
service-account*.json
id_rsa
id_ed25519

# os / logs
.DS_Store
*.log
